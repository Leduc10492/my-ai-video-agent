# Quick Start

直接告诉主控 Agent 当前目标和场景范围即可。

```text
把这份故事整理成短片剧本。
```

```text
读取最新剧本，为 scene-081 做分镜拆解，先保留完整 Shot Row，不生成图片或视频。
```

```text
根据已确认的分镜拆解生成 scene-081 的中文 Prompt 和 HTML。
```

```text
检查 scene-081：原台词覆盖、切镜理由、Prompt 打包、实际时长和 HTML 映射。
```

主流程默认停在 HTML。需要预演图或视频时，再明确指定 Prompt ID 和生成范围。
