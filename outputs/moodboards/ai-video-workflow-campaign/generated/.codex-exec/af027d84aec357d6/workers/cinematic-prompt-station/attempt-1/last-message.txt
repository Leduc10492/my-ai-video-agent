Native image generation ran, but the tool did not expose a saved PNG path or file handle I could copy to the requested `image.png` location.

I wrote the required failed `result.json` at the requested path with a concise error.