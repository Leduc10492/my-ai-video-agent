{
  "name": "@ai-director/agent-runtime",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "pnpm --filter @ai-director/skill-runtime build && tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @ai-director/skill-runtime build && vitest",
    "test:run": "pnpm --filter @ai-director/skill-runtime build && vitest run"
  },
  "dependencies": {
    "@ai-director/skill-runtime": "workspace:*",
    "zod": "^3.25.67"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
